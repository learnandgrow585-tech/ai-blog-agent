# AI Blog Agent — Streamlit Cloud requirements
# Streamlit Cloud installs this file automatically on deploy.

# ── Streamlit UI ──────────────────────────────────────────
streamlit>=1.35.0

# ── Core AI ───────────────────────────────────────────────
google-generativeai>=0.8.0     # Gemini API (free)
groq>=0.9.0                    # Groq API (free, ultra-fast)

# ── Environment & HTTP ────────────────────────────────────
python-dotenv>=1.0.0           # reads .env file
requests>=2.31.0               # HTTP calls to APIs

# ── Voice (edge-tts uses asyncio — nest_asyncio patches it) ──
edge-tts>=6.1.9                # Microsoft AI voices (free, no API key)
nest_asyncio>=1.6.0            # lets asyncio run inside Streamlit's event loop

# ── Video Assembly ────────────────────────────────────────
moviepy>=1.0.3                 # video editing (free)
Pillow>=10.0.0                 # image processing
